{
  "name": "tactictoe",
  "private": true,
  "packageManager": "pnpm@9.15.4",
  "scripts": {
    "test": "pnpm -r test",
    "build": "pnpm -r build",
    "dev:server": "pnpm --filter game-server dev"
  },
  "devDependencies": {
    "typescript": "^5.4.0"
  }
}
